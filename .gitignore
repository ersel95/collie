.build/
.swiftpm/
xcuserdata/
.DS_Store
DerivedData/
Icon

# Android (Android/) — the Gradle build lives beside the Swift package.
build/
.gradle/
.kotlin/
local.properties
captures/
*.iml

# Firebase client config. Points at a specific (here: corporate) project, so it is a local
# file: the example falls back to its HTTPS transport when this is absent, and CI builds it
# that way. See Android/example/README.md.
google-services.json
