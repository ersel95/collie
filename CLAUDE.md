# Collie

The full guide lives in `AGENTS.md` — read it first for integration, development rules,
and the behaviors that must be preserved. Integration details: `INTEGRATION.md` + the
template to copy: `Integration/CollieIntegration.swift`.
